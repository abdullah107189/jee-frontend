const config = {
  theme: {
    extend: {
      colors: {
        "surface-raised": "#131824",
        "text-primary": "#F5F7FA",
        "text-muted": "#8D96AC",
        "accent-signal": "#4F7CFF",
        "accent-deal": "#FF6B4A",
      },
      fontFamily: {
        display: ["var(--font-display)", "sans-serif"],
        body: ["var(--font-body)", "sans-serif"],
      },
    },
  },
};

export default config;
